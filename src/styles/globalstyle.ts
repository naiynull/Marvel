import { StyleSheet } from "react-native"

export const colors = {
    preto: '#000',
    branco: '#FFF',
    vermelho: '#861d1d',
    transparente: 'hsla(0, 100%, 100%, 0.25)',
}

export const styleContainer = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    }

})